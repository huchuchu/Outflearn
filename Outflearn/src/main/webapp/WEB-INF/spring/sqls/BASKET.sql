DROP
	TABLE
		BASKET;

CREATE
	TABLE
		BASKET(USER_NUM NUMBER REFERENCES USER_INFO(USER_NUM),
		CLASS_NUM NUMBER REFERENCES CLASS_INFO(CLASS_NUM) NOT NULL);

SELECT
	*
FROM
	BASKET;

INSERT
	INTO
		BASKET
	VALUES(2,
	1);
