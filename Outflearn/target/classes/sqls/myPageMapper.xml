<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="myPage">
	
	<select id="getBasketClass" resultType="ClassInfoDto" parameterType="int">
		SELECT * FROM CLASS_INFO WHERE CLASS_NUM IN(SELECT CLASS_NUM FROM BASKET WHERE USER_NUM = #{user_num})
	</select>
	
	<select id="getSubscribe" resultType="ClassInfoDto" parameterType="int">
		SELECT * FROM CLASS_INFO WHERE CLASS_NUM IN(SELECT CLASS_NUM FROM SUBSCRIBE WHERE USER_NUM = #{user_num})
	</select>
	
	<select id="getSubRoadmap" resultType="ClassInfoDto" parameterType="int">
		SELECT * FROM ROADMAP_INFO
	</select>
	
	<select id="getPreBasketClass" resultType="ClassInfoDto" parameterType="int">
		<![CDATA[ SELECT * FROM CLASS_INFO WHERE CLASS_NUM IN(SELECT CLASS_NUM FROM BASKET WHERE USER_NUM = #{user_num}) AND ROWNUM < 6 ]]>
	</select>
	
	<select id="getPreSubscribe" resultType="ClassInfoDto" parameterType="int">
		<![CDATA[ SELECT * FROM CLASS_INFO WHERE CLASS_NUM IN(SELECT CLASS_NUM FROM SUBSCRIBE WHERE USER_NUM = #{user_num}) AND ROWNUM < 6 ]]>
	</select>
	
	<select id="getPreSubRoadmap" resultType="ClassInfoDto" parameterType="int">
		<![CDATA[ SELECT * FROM CLASS_INFO WHERE ROWNUM < 6 ]]>
	</select>

</mapper>